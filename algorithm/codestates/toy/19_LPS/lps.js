const LPS = function (str) {
  // TODO: 여기에 코드를 작성합니다.
  let prefixIdx = 1;
  let suffixIdx = str.length - 1;
  let result = 0;
  let prefixStr = '';
  let suffixStr = '';

  return result;
};

let output = LPS('abbbcc');
console.log(output);

output = LPS('aaaa');
console.log(output);

output = LPS('aaaaa');
console.log(output);